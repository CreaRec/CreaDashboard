{
  "name": "crea-dashboard",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "setup": "node scripts/setup-dev.js",
    "dev:servers": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev": "npm run setup && npm run dev:servers",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "node scripts/setup-db.js && npm run start -w backend",
    "db:up": "docker compose up -d postgres",
    "db:migrate": "npm run db:migrate:deploy -w backend",
    "db:seed": "npm run db:seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
